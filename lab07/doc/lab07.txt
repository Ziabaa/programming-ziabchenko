Лабораторна робота №7. Функції

Зябченко Іван Володимирович, гр. КН-922а

Завдання:Переробити програми, що були розроблені під час виконання лабораторних робіт з тем "Масиви" та "Цикли" 
таким чином, щоб використовувалися функції для обчислення результату.


Основна частина: 
	1.'Знайти за яку мінімальну кількість купюр можна витратити суму цифр'
	- опис роботи основної функції: код для цієї функції повинен виглядати наступним чином: 

	int number = 34;//дане нам число
	int quantity = 0;//кількість купюр що знадобиться
	int sum = 0;//сума для підрахунку
	int nominal_money1 = 10;//перша купюра
	int nominal_money2 = 5;//друга купюра
	int nominal_money3 = 2;//третя купюра
	int nominal_money4 = 1;//четверта купюра
	while (sum < number) {//виконується до моменту поки сума не бульша за дане число
		if (sum + nominal_money1 <= number) {//якщо поточна сума більша за дане число, то
			sum += nominal_money1;//до суми додати ноиінал першого числа 
			quantity++;//та збільшити кільквсть на 1
		} else if (sum + nominal_money2 <= number) {//якщо поточна сума більша за дане число, то
			sum += nominal_money2;//до суми додати номінал другого числа
			quantity++;//та збільшити кільквсть на 1
		} else if (sum + nominal_money3 <= number) {//якщо поточна сума більша за дане число, то
			sum += nominal_money3;//до суми додати номінал третього числа
			quantity++;//та збільшити кільквсть на 1
		} else if (sum + nominal_money4 <= number) {//якщо поточна сума більша за дане число, то
			sum += nominal_money4;//до суми додати номінал четвертого числа
			quantity++;//та збільшити кільквсть на 1
		}
	}
	- для того щоб зробити цей код функцією, потрібно:
	-обов'язковим кроком є попереднє оголошення функції, яке виглядає як початок функції, без наповнення.
	- першим кроком потрібно визначити, яка повинна бути функція та які значення потрібні для розрахунка.
	 Отже функція приймає та віддає цілі значення, отже це 'int', значення що потрібні для розрахунка, це
	 будь-яке число яке потрібно задати у дужках. Вигляд: 
			int how_many_bancnotes_will_be_needed(int number);
	- другим кроком вставляємо наповнення. 
	- третім кроком визначаємо, що повинна повертати функція на віході.
	 Повинна повернути ціле число, а саме кількість купюр, саме тому виглядає так в кінці:
			return quantity;
	2.'У масиві цілих чисел визначити суму двозначних чисел.'
	- опис роботи основної функції: код для цієї функції повинен виглядати наступним чином: 
		int suma = 0;
		for (int i = 0; i < size; i++) { //виконується доки поточне число не більше за останнє
		  if (arrаy[i] >= 10) { //якщо дане число більше або дорівнює 10
		    suma += arrаy[i]; //то до суми додаємо поточне число
		  }
		}
	- для того щоб зробити цей код функцією, потрібно:
	-обов'язковим кроком є попереднє оголошення функції, яке виглядає як початок функції, без наповнення.
	- першим кроком потрібно визначити, яка повинна бути функція та які значення потрібні для розрахунка.
	 Отже функція приймає та віддає цілі значення, отже це 'int', значення що потрібні для розрахунка, це
	 будь-яке число яке потрібно задати у дужках. Вигляд: 
			int sum_dooble_number_of_arrаy(int arrаy[], int size);
	- другим кроком вставляємо наповнення. 
	- третім кроком визначаємо, що повинна повертати функція на віході.
	 Повинна повернути ціле число, а саме суму двоцифрових чисел, саме тому виглядає так в кінці:
			return suma;
- дослідження результатів роботи програми:
	-Вводимо значення для перевірки:
		#define ROZM 5 //кількість чисел у масиві
		int my_arrаy[ROZM] = { 34, 12, 24, 7, 4 }; //даний масив
		int sum = sum_dooble_number_of_arrаy(my_arrаy, ROZM); //виконання функції: знаходження суми двоцифрових чисел
		unsigned int bancnotes = how_many_bancnotes_will_be_needed(34); //мінімальна кількість купюр у сумі цифр
	-Повертаємо 0(правильність виконання):
		return 0;
	-Запускаємо програму та зупиняємось на 'return 0' та введемо команду "fr v".
	 В результаті помилок немає, всі результати зійшлись.
Структура проекту лабораторної роботи:
.
└── lab07
      ├── README.txt
      ├── doc
      |    └── lab07.txt
      ├── Makefile
      └── src
	   └── main.c
Висновки: при виконанні лабораторної роботи були набуті практичні навички створення та системи роботи функцій, та вивчено новий 
розділ: функції. Також покращено навички використання компілятора, масивів, умов, циклами та в мові С взагалі.
